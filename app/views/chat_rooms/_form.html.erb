<%= form_for(@chat_room) do |f| %>
  <% if @chat_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chat_room.errors.count, "error") %> prohibited this chat_room from being saved:</h2>

      <ul>
      <% @chat_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :master_id %><br>
    <%= f.text_field :master_id %>
  </div>
  <div class="field">
    <%= f.label :max_count %><br>
    <%= f.number_field :max_count %>
  </div>
  <div class="field">
    <%= f.label :admission_count %><br>
    <%= f.number_field :admission_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
